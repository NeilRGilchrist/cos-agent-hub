# Example doc spec — replace with your project's real client document(s).
#
# A doc spec is a READ-MODEL SELECTOR: it names the FRs and ADRs that make up
# one client-facing document, by ID only. It holds NO prose — every section of
# the rendered document is generated by scripts/render-docs.py from the spec
# graph. See README.md in this directory for the schema and the guard rule.
doc: overview
title: "Project overview"
frs:
  - FR-0001
adrs: []
